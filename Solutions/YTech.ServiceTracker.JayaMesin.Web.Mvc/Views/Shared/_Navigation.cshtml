@{ Layout = null; }

@(Html.Kendo().PanelBar()
    .Name("navigationPanelBar")
    .Animation(true)
    .ExpandMode(PanelBarExpandMode.Single)
    .HighlightPath(true)
    .SelectedIndex(0)
    .Items(panels =>
    {
        panels.Add()
           .Text("Master")
           .Items(childPanels =>
           {
               childPanels.Add().Text("Konsumen").Action("Index", "Customer");
           });
        
        panels.Add()
            .Text("Work Order")
            .Items(childPanels =>
            {
                childPanels.Add().Text("Daftar Work Order").Action("Index", "WO");
            });
    })
)