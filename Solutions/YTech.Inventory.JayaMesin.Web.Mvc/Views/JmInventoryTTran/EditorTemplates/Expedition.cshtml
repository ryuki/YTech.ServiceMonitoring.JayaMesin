@model string

@(Html.Kendo().DropDownListFor(m => m)
        .OptionLabel("-Pilih Ekspedisi-")
        .DataValueField("Id")
        .DataTextField("ExpeditionName")
        //.BindTo((System.Collections.IEnumerable)ViewData["customers"])
         .HtmlAttributes(new { @style="width:200px;" })
         .AutoBind(true)
         .DataSource(source => {
              source.Read(read =>
              {
                  read.Action("PopulateJmInventoryMExpeditions", "JmInventoryMExpedition");                  
              });              
          })
)